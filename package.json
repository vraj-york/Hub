{
	"name": "july-hub-socrates",
	"private": true,
	"description": "Developer integration: Hub Socrates app under ./Frontend, Launchpad reference at ./launchpad-frontend",
	"scripts": {
		"dev": "npm run dev --prefix Frontend",
		"build": "npm run build --prefix Frontend"
	}
}
